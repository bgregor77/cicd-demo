---
applications:
- name: core-cf-microservice-bg
  buildpacks:
    - dotnet_core_buildpack
  stack: cflinuxfs3
  instances: 1
  memory: 256M
  env:
    ASPNETCORE_ENVIRONMENT: qa
  services:
    - bg-test-config
    - bg-test-svc-registry
